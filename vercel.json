{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.py"
        }
    ],
    "installCommand": "python -m pip install --upgrade pip setuptools wheel && python -m pip install ./HomepageBuilder-0.14.5"
}